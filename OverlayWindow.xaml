<Window x:Class="NativeOverlayTranslator.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="CanResizeWithGrip"
        Topmost="False"
        ShowActivated="False"
        ShowInTaskbar="False"
        SizeToContent="Manual"
        MinWidth="16"
        MinHeight="12">
    <Border x:Name="Frame"
            Background="#EAF7F7F2"
            BorderBrush="#4A2F3437"
            BorderThickness="1"
            CornerRadius="2"
            Padding="2">
        <Grid>
            <TextBox x:Name="TranslationBox"
                     MinWidth="0"
                     MinHeight="0"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     TextAlignment="Center"
                     VerticalContentAlignment="Center"
                     VerticalScrollBarVisibility="Disabled"
                     HorizontalScrollBarVisibility="Disabled"
                     Background="Transparent"
                     BorderThickness="0"
                     Padding="0,-1,0,1"
                     Foreground="#101418"
                     FontSize="18"
                     TextChanged="TranslationBox_OnTextChanged"
                     PreviewMouseLeftButtonDown="TranslationBox_OnPreviewMouseLeftButtonDown" />
        </Grid>
    </Border>
</Window>
